<div class="wrapper">
  <div class="container">
    <% if @score == true %>
    CORRECT
    your score : <%= @results["length"] %>
    <% else  %>
    That word does not exist!
    <% end  %>
    <%= link_to("play again?", "/new") %>
  </div>
</div>
